<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.hellomaul.simplecrud">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".guru.guruactivity"></activity>
        <activity android:name=".guru.manage_guru" />
        <activity android:name=".guru.guru_list" />
        <activity android:name=".inventaris.Inventaris_Activity" />
        <activity android:name=".inventaris.Manage_Inventaris" />
        <activity android:name=".inventaris.inventaris" />
        <activity android:name=".pegawai.pegawai_activity" />
        <activity android:name=".pegawai.manage_pegawai" />
        <activity android:name=".student.ManageStudentActivity" />
        <activity android:name=".student.MainActivity" />
        <activity android:name=".simplecrud">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>